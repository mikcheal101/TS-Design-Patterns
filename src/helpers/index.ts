// alert the mediator that the value has changed
const randomizing = (maximumNumber: number, minimumNumber: number = 0) => Math.floor(Math.random() * (maximumNumber - maximumNumber + 1)) + minimumNumber;
